syntax = "proto3";

option csharp_namespace = "Messenger2ClientApp";

package messenger;

message Request{  
    string content = 1; 
} 

message Response{  
    string content = 1; 
} 

service Messenger{ 
  // client-side streaming 
  rpc ClientDataStream (stream Request) returns (Response); 
} 