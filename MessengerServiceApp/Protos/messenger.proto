syntax = "proto3";

option csharp_namespace = "MessengerServiceApp";

package messenger;

message Request{ } 

message Response{ 
    string content = 1; 
} 

service Messenger{ 
  // server-side streaming 
  rpc ServerDataStream (Request) returns (stream Response); 
}